---
"@nanocollective/nanocoder": minor
---

Pasted-text placeholders (`[Paste #N]`) now behave as single atomic units when editing — the cursor can't land inside one, backspace removes the whole placeholder, and freshly typed characters no longer merge into it. Chat history also shows the real pasted text, with a display cap for very long pastes.
